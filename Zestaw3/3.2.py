# L = [3, 5, 4]; L = L.sort()
# Podany kod nie jest poprawny, ponieważ L nic nie zwraca

# x, y = 1, 2, 3
# Podany kod nie jest poprawny, poniewaz mamy za mało zmiennych względem przypisywanych wartości

# X = 1, 2, 3 ; X[1] = 4
# Podany kod nie jest poprawny, ponieważ  krotki nie obsługują przypisywania elementów

# Podany kod nie jest poprawny, ponieważ nie posiadamy w liście miejca na element z indeksem 3
# X = [1, 2, 3] ; X[3] = 4

#Podany kod nie jest poprawny, poniewaz funkcja append dodaje element do listy, a nie stringa
# X = "abc" ; X.append("d")

#Podany kod nie jest poprawny, poniewaz funkcja power powinna zawierac dwa argumenty
# L = list(map(pow, range(8)))
